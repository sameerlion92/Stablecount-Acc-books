services:
  accbooks:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      STABLECOUNT_DATA_DIR: /data
    volumes:
      - stablecount-data:/data

volumes:
  stablecount-data:
